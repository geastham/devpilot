node_modules
.env
.env.local
.env*.local

# Build artifacts / caches (should never be committed)
.next/
out/
*.tsbuildinfo
.turbo/

# Local runtime database created by db:push / devpilot init
/.devpilot/
*.db-shm
*.db-wal

# Built cockpit bundle for the CLI. ~32 MB of traced Next output — produced by
# `pnpm --filter @devpilot.sh/cli bundle:cockpit` at publish time, never committed.
packages/cli/ui/
